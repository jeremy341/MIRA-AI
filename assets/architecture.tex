\documentclass[border=10pt]{standalone}
\usepackage[ngerman]{babel}
\usepackage{tikz}
\usetikzlibrary{arrows.meta}

\begin{document}
\begin{tikzpicture}[
    box/.style={rectangle, draw=white, fill=#1, rounded corners, minimum width=6cm, minimum height=0.9cm, align=center, font=\footnotesize, text=white},
    arrow/.style={thick, ->, >=stealth, white}
]
    \node (input) [box=black] {Bild oder USB-Kameraframe};
    \node (model) [box=black, below of=input, node distance=1.4cm] {Ultralytics-kompatible Detektion (\texttt{.pt} oder TFLite)};
    \node (detections) [box=black, below of=model, node distance=1.4cm] {Begrenzungsrahmen, Klassen und Konfidenzen};
    \node (tracker) [box=black, below of=detections, node distance=1.4cm] {Optional: ByteTrack für \texttt{.pt}-Modelle};
    \node (api) [box=black, below of=tracker, node distance=1.4cm] {FastAPI Control Center (REST und WebSocket)};
    \node (browser) [box=black, below of=api, node distance=1.4cm] {Browser-Dashboard zur Beobachtung und Konfiguration};

    \draw [arrow] (input) -- (model);
    \draw [arrow] (model) -- (detections);
    \draw [arrow] (detections) -- (tracker);
    \draw [arrow] (tracker) -- (api);
    \draw [arrow] (api) -- (browser);
\end{tikzpicture}
\end{document}
